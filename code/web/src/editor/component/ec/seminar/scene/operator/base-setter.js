/* eslint-disable no-unused-vars */

import Element from '@ux/base/element';

/**
 * 设置基类
 */
export default class BaseSetter extends Element {
    /**
     * 
     * 构造函数
     * 
     * @param {*} tag_name 
     */
    constructor(tag_name) {
        super(tag_name)
    }
}
