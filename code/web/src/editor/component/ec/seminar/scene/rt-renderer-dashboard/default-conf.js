/* eslint-disable no-unused-vars */

import __default_hdr       from '@/assets/hdr/near_the_river_1k.hdr';
import __default_hdr_thumb from '@/assets/images/hdr-thumb/near_the_river_1k.jpg';

/**
 * 默认的配置
 */
export default {
    /**
     * 获取
     */
    get default_hdr() {
        return __default_hdr;
    },

    /**
     * 获取
     */
    get default_hdr_thumb() {
        return __default_hdr_thumb;
    },
}
