
1. 只有 

Texture
BufferGeometry
Material
EditableMesh_Bucket

这三个JS对象存在引用计数

    1. 这4个对象，都需要通过retain/release来维持引用计数，dispose是强制释放，会导致对象直接变空
    2. 初始化引用计数为 1
    3. 这4个对象，都存在clone(deep)函数，支持深拷贝或者浅拷贝

2. 所有的Wasm对象都有引用计数

notice: mesh 不支持引用计数，但是内部的geo和material 支持引用计数
