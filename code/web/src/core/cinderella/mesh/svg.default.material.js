/* eslint-disable no-unused-vars */

import AspectColor from '@xthree/material/aspect-color';

/**
 * 
 * 构造
 * 
 * @returns 
 */
export default function() {
    const material = new AspectColor();
    material.setFrontColor(0x868686);
    return material;
}

